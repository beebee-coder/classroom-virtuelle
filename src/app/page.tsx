// src/app/page.tsx
import { HomeClient } from '@/components/HomeClient';

// Cette page est maintenant un simple wrapper qui rend le composant client.
export default function HomePage() {
  return <HomeClient />;
}
