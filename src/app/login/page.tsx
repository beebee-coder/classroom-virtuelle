// src/app/login/page.tsx
import { LoginView } from './LoginView';

// This is the main server component for the page.
// Its only job is to render the client view.
export default function LoginPage() {
  return <LoginView />;
}
